import { handleSubmit } from "../src/client/js/app"
describe("The function should be defined", () => {
    test("Testing the handleSubmit() function", () => {
           expect(handleSubmit).toBeDefined();
    })
});